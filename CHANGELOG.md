# @raycas/nanostorage

## 0.1.0

### Minor Changes

- 6f26948: Add `watchAll(area, listener)` to the core API to subscribe to all key mutations within a storage area (`local` or `session`) with a single listener.

  ## Improve wildcard listener routing in pubsub and add unit/e2e coverage for same-tab and cross-tab behavior.

  ## "@raycas/nanostorage": minor

  Add `watchAll(area, listener)` to the core API to subscribe to all key mutations within a storage area (`local` or `session`) with a single listener.

  Improve wildcard listener routing in pubsub and add unit/e2e coverage for same-tab and cross-tab behavior.

### Patch Changes

- e78c360: Refactor React adapter into smaller modules without changing the public API.

## 0.1.0-beta.3

### Patch Changes

- Refactor React adapter into smaller modules without changing the public API.

## 0.1.0-beta.2

### Minor Changes

- Add `watchAll(area, listener)` to the core API to subscribe to all key mutations within a storage area (`local` or `session`) with a single listener.

  Improve wildcard listener routing in pubsub and add unit/e2e coverage for same-tab and cross-tab behavior.

## 0.0.1

### Patch Changes

- update config
